#include "min_heap.h"

template <typename T>
void Heap<T>::heapify(Node* root) {
}

template <typename T>
T Heap<T>::top() {
	return root->data;
}

template <typename T>
void Heap<T>::pop() {
}

template <typename T>
void Heap<T>::push(T data) {
}


